# 斐波那契数列
```
def fib(n):
	pre, curr = 0, 1 # 设定初始值
	k = 2 # 索引值 = 进入循环前/后，当前curr是第k位fib
	
	# 只有k还没到目标第n位才运行，当k已经到达第n位，curr就是第n位fib
	# p = c, c = p + c两等式是同时计算的，联想a = a + 1
	# 完成一次计算即数列前进一位，k++
	while (k < n):
		pre, curr = curr, pre + curr 
		k += 1
	
	return curr
	
```