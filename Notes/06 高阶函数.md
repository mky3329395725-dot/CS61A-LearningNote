> 编程语言的一种特性
> 接受函数作为参数或者其返回值为参数的函数

- 以函数作为参数
	1. 有共同特点的几个函数，提取出其共同特点（控制与操作）
	2. 写一个满足它们共同特点（控制与操作）的函数
	3. 在各自写几个函数时，先调用共同特点函数，再写其独有的特性
	4. 这样就可以避免在几个函数中，都重复写入这个共同特点
- 以函数作为返回值（*即时间创建函数*）
	1. 在函数中创建函数，并返回这个函数中的函数